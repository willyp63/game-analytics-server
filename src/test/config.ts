// Test configuration
export const TEST_CONFIG = {
  // Test database name
  DB_NAME: "test",

  // Test timeout
  TIMEOUT: 30000,

  // Test port (different from main app to avoid conflicts)
  PORT: 3001,

  // Supported games for testing
  SUPPORTED_GAMES: ["tetris", "snake", "pong", "breakout", "pacman"],
};
